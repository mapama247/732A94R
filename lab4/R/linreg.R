# to be completed :(
# roxygen documentation HERE

linreg <- function(formula,data){
	#1st) Use the function model.matrix() to create the matrix X (independent variables
	#2nd) Pick out the dependent variable y using all.vars()
	#3rd) Do the calculations using ordinary linear algebra (check formulas from pdf)
	return(TRUE)
}

# The formula argument should take a formula object.
# The function should return an object with of class linreg either as an S3 class or an RC class.